#!/usr/bin/env node

const mock = require('mock-require');

mock('analyze-deps', '../mocks/analyze-deps-mock');
mock('find-nearest-file', '../mocks/find-nearest-file-mock');

require('../../bin/deps');
